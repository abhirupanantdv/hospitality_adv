[project]
name = "hospitality_adv"
authors = [
  { name = "Hospitality ADV", email = "admin@example.com" }
]
description = "ERPNext/Frappe backend app for Hospitality ADV hotel operations."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[tool.black]
line-length = 110

